type: light

duration: infinite

period: 500ms

variables:
  index:
    loop:
      count:  config.light.count
      period: 500ms

scenes:
  one:
   type: insert
   offset: local.loop
   scene:
     type: spread
     begin: red
     end: white
     steps: 10

elements:
  -
  begin: 0
  duration: 10
  type: scene
  scene: one

