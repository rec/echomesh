type: mic

source: hofmann
target: hofmann

mapping:
  moderate:
    function: select_random
    arguments:
      - [play, filename]
      - [play, filename2]

  loud:
    function: select_random
    arguments:
      - [play, filename]

---

type: random

data:
  distribution: poisson
  mean: 10.0

mapping:
  event:
    function: select_random
    arguments:
      - [play, filename]
      - [play, filename2]
